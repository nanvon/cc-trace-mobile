# Q4 采集记录

仅记录已经完成的验证。响应 JSON 保持为可直接喂给解析器的纯数据文件，采集元数据不写进 JSON。

| 文件 | 日期 | Provider | 账号类型 | scope | 结果 |
|---|---|---|---|---|---|
| `fixtures/raw/codex-usage-1785231427625.json` + `.headers.txt`（本地忽略） | 2026-07-28 | Codex | OAuth 测试账号（计划名仅在原始响应中，未写入版本库） | `openid profile email offline_access api.connectors.read api.connectors.invoke` | token / usage HTTP 200；`account_id` 来自 access token claim；发现桌面 fixture 未覆盖的 `credits` 与 reset-credit 字段 |
| `fixtures/raw/claude-usage-1785231478487.json` + `.headers.txt`（本地忽略） | 2026-07-28 | Claude | OAuth 测试账号（计划名待后续脱敏确认） | `org:create_api_key user:profile user:inference user:sessions:claude_code user:mcp_servers user:file_upload` | token / usage HTTP 200；跨域 token 交换成立；发现 `extra_usage`、`spend`、动态窗口元数据；5 小时 `is_active` 观察已开始 |
| `fixtures/raw/codex-usage-1788087130391.json` + `.headers.txt`（本地忽略） | 2026-08-30 | Codex | OAuth 本人账号 | `openid profile email offline_access api.connectors.read api.connectors.invoke` | token / usage HTTP 200；确认 `credits`（含 `balance`、`has_credits`、`unlimited`、`overage_limit_reached`）与内嵌的 `rate_limit_reset_credits` 摘要真实存在，据此推翻[展示要求 §3.3](../docs/移动端额度展示要求.md) 的「找不到数据来源」 |
| `fixtures/raw/claude-usage-1788087161219.json` / `claude-profile-1788087161713.json` + `.headers.txt`（本地忽略） | 2026-08-30 | Claude | OAuth 本人账号 | `org:create_api_key user:profile user:inference user:sessions:claude_code user:mcp_servers user:file_upload` | token / usage / profile 均 HTTP 200；确认 `spend` 的 `used` / `limit` / `percent` 可用，而 `balance`、`auto_reload` 为 `null`，`can_purchase_credits`、`can_toggle` 为 `false`——账户余额在 OAuth 链路上取不到（[§4.3](../docs/移动端额度展示要求.md)）。另见一批未文档化的代号窗口键，样本中几乎全为 `null` |

## 尚未脱敏入库

上面两次采集只留在 Git 忽略的 `fixtures/raw/`（权限 `0600`）。它们含真实邮箱、
account uuid 与**账号实际额度和余额数值**，按 [`verify/oauth/README.md`](../verify/oauth/README.md)
必须先人工白名单过滤才能进 `fixtures/providers/`；是否连真实数值一起入库需要单独决定，
本次未做。


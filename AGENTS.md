# cc-trace-mobile AI 协作规范

本文件约束参与 `cc-trace-mobile` 仓库的 AI。它规定**怎么工作**；产品与技术事实由 `docs/` 拥有。

## 1. 当前阶段

**开发优先、真机验收待执行。** 因目标真机条件暂不具备，用户于 2026-07-29 明确推翻原先
“Q3 通过前不写正式产品”的开发顺序，要求先按原型完成所有可静态开发的功能。S2–S6
代码与自动化测试现已落地，Android Debug APK 已构建；这些结果不等于 Provider 实调或
双平台真机验证通过，详见
[开发实现与静态验证记录](docs/开发实现与静态验证记录.md)。

当前应用的默认入口是正式用量主屏；原 Q3 验证工具保留在 **Debug 构建的设置页**，不得进入
Release。后续主线是完成 [OAuth 可行性验证](docs/OAuth可行性验证.md) 的 Q3 真机矩阵及
Q4 剩余实调项，并据真实结果修正当前实现中的待验证假设。

Q3 仍必须在当前 Flutter 应用内完成真机验证，不能由静态检查、模拟器或 macOS 命令行
脚本替代。Debug 验证页的验收范围和未完成项以
[S1 / Q3 专项计划](docs/S1-Q3真机验证专项计划.md)为准。

## 2. 通用规则

- 未经用户明确要求，不改代码；默认只做分析、定位、方案与风险说明。
- 未经用户明确要求，不运行 build、lint、test、dev 或平台构建。
- 只做任务真正需要的最小改动，不顺手重构无关代码。
- 没有验证过的结论不得写成已验证；平台相关结论必须标注平台，未验证的平台明确写未验证。
- 无法确定的取值写成「> 待确认」，并说明假设错误会影响什么。
- 不为后续能力预留空页面、空导航、占位卡片或不可用入口。

## 3. 与桌面端的关系

[cc-trace](https://github.com/nanvon/cc-trace) 是独立仓库、独立应用。边界见 [ADR-0001](docs/决策/ADR-0001-独立仓库与技术栈选型.md)。

**沿用它的语义，不沿用它的代码结构。** 具体地：

- Provider 接口路径、字段解析、额度归一化 → 以桌面端 `docs/额度领域模型.md` 和 `src-tauri/src/providers/` 为准，那是真机验证过的
- 状态语义 → 沿用三维契约（活动 / 快照新鲜度 / 失败原因），不自造 `fresh/stale/error` 三值枚举
- 设计 token、余量四档色 → 沿用；紧凑面板的 `380 × 392` 布局**不要照搬**，那是给菜单栏挂件设计的密度

`docs/cc-bar-reference/10-移动端获取 Codex 与 Claude 用量.md` 是对 Nowdex 安装包**静态字符串**的观察，证据等级最低。**只有 OAuth 一节可以参考**（桌面端不做登录，那部分无可替代），其余内容不得当作接口依据。

## 4. 安全纪律

从验证阶段就开始守，不等到写正式代码：

- access token、refresh token、authorization code 不进日志、不进前端、不进缓存、不进 URL 之外的任何地方
- 持有 token 的类型不得通过 `toString`、日志或调试输出暴露；独立 Rust 验证工具仍须手动
  屏蔽 `Debug`
- 真实响应存为**脱敏** fixture；原始响应不入库
- 只读额度，不读取、不上传对话内容
- 不把凭据发送到任何自有服务器

## 5. 继承的硬约束

- **不得提出任何以购买 Apple 开发者账号或代码签名证书为前提的方案**（继承 cc-trace ADR-0016）。iOS 侧用免费 Apple ID 真机调试，接受 7 天重签；Android 直接分发 APK。

## 6. 发布

版本号真值在 `pubspec.yaml` 的 `version:`（`versionName+versionCode`）。发布 tag 必须是 `v<versionName>`，由 `scripts/check_release_version.sh` 在 release 流程中强制校验；README 的 release badge 自动跟随最新 Release，不用手改。产物、Secrets 与验收细节以 [GitHub 自动构建与发布](docs/GitHub自动构建与发布.md) 为准。

### 更新说明

每个版本的 Release 正文来自手写的 `release-notes/v<versionName>.md`，格式与撰写规则见 [release-notes/README.md](release-notes/README.md)。CI 会校验该文件存在且非空，缺失就直接失败、不创建 Release；正文原样使用，末尾由 workflow 自动追加 Android／iOS 产物说明，不用手写。这不是聚合 `CHANGELOG.md`，每个版本只留一份面向用户的说明。

### 标准发布流程（用户说「发版」时）

「发版」是预先授权的固定发布指令，出现这句话（或「发布一下」「发个新版本」等同义表述）时按下面四步顺序执行，**不用逐步等确认**；这条不放宽第 2 节的其他规则，除发布本身所需的提交与推送外仍不改无关代码、不跑额外构建：

1. **提交代码**：把工作区未提交的改动按仓库惯例的 commit message 风格提交；无关改动分开提交，不要混在一起。
2. **编写用户更新说明**：对比上一个 `v*` tag 与当前代码、提交和文档，只提炼真实、用户可感知的新增功能、问题修复、体验优化和必要注意事项；不要罗列 commit、文件、重构、CI 或版本号。未经真机验证的改动不写成已验证。创建 `release-notes/vX.Y.Z.md`。
3. **版本号 +1**：改 `pubspec.yaml` 的 `version:`，versionName patch 位 +1、versionCode 同步 +1（versionCode 以 `2001` 为仓库基线逐版递增，如 `0.1.10+2011` → `0.1.11+2012`），除非用户明确要求升 minor/major。版本号和 `release-notes/vX.Y.Z.md` 一起单独提交，message 沿用仓库风格 `升级版本至 X.Y.Z`。
4. **触发远程构建发布**：推送分支 + 打带 `v` 前缀的 tag 触发 `.github/workflows/release.yml`：

   ```bash
   git push origin main
   git tag vX.Y.Z
   git push origin vX.Y.Z
   ```

- tag 必须和已提交的 versionName 一致，tag 本身不改版本号，只是给已提交好的版本打标记；打错可撤销：`git tag -d vX.Y.Z && git push origin :vX.Y.Z`。
- `git push origin vX.Y.Z` 成功即代表本地发版操作完成；只汇报版本号、提交和 Actions / Release 链接，**不得主动查询、等待或轮询** CI、Release 或产物状态。只有用户明确要求「看进度」「确认发布成功」或「检查产物」时，才查询远端状态。
- 用户只说「打个正式版」而工作区本来就干净时，照样按这四步走，第 1 步无改动则跳过。

## 7. 文档的效力

文档记录的是**当时已知条件下的最佳判断**，不是不可推翻的结论。

- 任何决策都可以推翻，包括本文件和已确认的 ADR。发现更好的方案时必须主动提出并给出明确推荐，不得因为「文档已经这么定了」就不提或降级成备选项。
- **理由必须是第一性的。** 「与 ADR-XXXX 冲突」只说明需要走推翻流程，它本身不构成反对理由。
- 推翻要显式：说清楚推翻什么、为什么现在的判断和当时不同、代价是什么，取得用户同意后再改；改完补或更新 ADR。
- 文档与现实不符时**以现实为准**，并同步修正文档。

这条不放宽第 2 节的「未经要求不改代码」和「最小改动」——可以随时推翻的是结论，不是流程。

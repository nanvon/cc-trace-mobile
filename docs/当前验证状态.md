# 当前验证状态

> 目的：本仓库**当前验证矩阵的唯一权威入口**。读者想回答「Android 哪些场景已验证、iOS 还缺
> 什么、当前版本是多少」，只看本文件即可。
>
> 历史记录（如 `开发实现与静态验证记录.md`）保留原日期、不改写，只作为当时时间线的证据；
> 任何验证完成或失败，先更新本文件，再同步 README / Release notes 的引用。
>
> 最后更新：2026-08-13 · 应用版本 0.1.6 (2007)

## 1. 当前版本基线

| 项 | 值 |
|---|---|
| 应用版本 | `0.1.6+2007` |
| Android 产物 | 正式签名 arm64-v8a APK + SHA-256 |
| iOS 产物 | Release 无签名编译通过（CI）；免费 Apple ID 自签真机调试，7 天重签 |
| 分发边界 | 不购买 Apple 开发者账号，无 App Store / TestFlight / IPA |

## 2. 静态与自动化验证

> 2026-08-13 在当前工作树（含 S1–S3 性能优化与 quotaCache v2）执行。

| 项 | 结果 | 能证明 | 不能证明 |
|---|---|---|---|
| `flutter analyze` | 通过 | 编译期与 lint 无问题 | 运行时行为 |
| `flutter test`（全量） | 通过 | 解析、OAuth 参数/回调、token refresh、请求头、退避、缓存、状态 UI 契约 | 真机行为与视觉观感 |
| `flutter test`（golden） | 本机失败 | — | 本机字体渲染与 golden 生成环境存在 0.01%（20–37 px）差异，**与代码改动无关**（stash 对照后仍失败）；需在 golden 生成环境复验 |
| `flutter build apk --debug` | 成功 | Kotlin / Dart 可编译（含 Custom Tabs release 通道、可取消 HTTP） | 真机 Custom Tabs 生命周期与弱网行为 |

更早的自动化证据（2026-07-29 / 07-30 工作树）见 `开发实现与静态验证记录.md` §3，只代表当时时间线。

## 3. 真机验证

### Android

| 场景 | 状态 | 说明 |
|---|---|---|
| 登录 + 额度实调端到端 | **有声明、缺记录** | README 声称已在真机完成，但缺少版本、设备、系统版本、日期与场景细节；不视为 Q3 矩阵闭环，需补齐证据后登记 |
| Custom Tabs 会话 / localhost 回跳 / 取消 / 超时 / 重复回调 | 未验证 | 按 `S1-Q3真机验证专项计划.md` |
| Keystore 重启 / 卸载、退后台 HttpServer | 未验证 | 同上 |

### iOS

| 场景 | 状态 |
|---|---|
| 免费 Apple ID 自签真机安装 | 未验证 |
| `ASWebAuthenticationSession`、loopback 生命周期、Keychain 重启/卸载 | 未验证 |
| 7 天重签流程 | 未验证 |

### Provider / 协议

| 场景 | 状态 |
|---|---|
| Q4 最小 scope 削减实验 | 未完成（Claude 侧优先：`org:create_api_key`、`user:file_upload`） |
| 真实 429 `Retry-After`、最小安全刷新间隔 | 未完成 |
| Claude `is_active` 精确语义 | 仅协议观察项 |

详见 `OAuth可行性验证.md`。

## 4. 更新规则

- 真机或真实账号验证完成后，把证据（版本、设备、系统、日期、场景、结果）登记到 §3，再更新 README 与 Release notes。
- 自动化结果随每次工作树变更重跑后更新 §2。
- 同一事实只能有一个当前状态；历史记录不得反向冒充当前状态。
